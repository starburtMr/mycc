# 规划优化记录（v1.2）

## 1. 本轮已落实

1. 新增 `00-基线/技术栈冻结-v1.2.md`。
2. 新增 `02-契约/API契约草案-v1.2.md`。
3. 新增 `02-契约/事件字典-v1.2.md`。
4. 强化 `01-架构/后端重设计-v2.md`：补了 event 事务顺序和 adapter 最小接口。
5. 强化路线图：补了“恢复后不丢不重不乱序”与 snapshot 实现约束。
6. 强化任务拆解：新增 replay fixtures、备份恢复演练任务。

## 2. 待拍板（需你确认）

1. `chat/send` 是否强制“总是创建 task run”？
- 当前 v1.2 采用“是”，便于统一账本与回放。

2. SSE `after` 是否只允许 `seq`（不再支持 event_id）？
- 当前 v1.2 采用“是”，实现更简单稳定。

## 2.1 拍板结果

1. `chat/send`：确认采用“统一进入 task run 账本”。
2. `after`：确认只支持 `seq`。

## 3. 建议决策

1. 采用“chat 统一入 run”。
2. 采用“after 只用 seq”。
3. 这两条确认后，再把 v1.2 设为默认开发契约。

---

状态：待拍板
