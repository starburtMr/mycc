---
name: your-skill-name
description: 说明这个 skill 在什么情况下触发、解决什么问题、输出什么结果。
---

# your-skill-name

## 触发场景

- 用户提出与本 skill 明确匹配的需求时触发。
- 若需求不匹配，回退到系统默认流程。

## 输入要求

1. 目标与约束。
2. 上下文文件路径（如任务文件、项目 context）。
3. 需要调用的工具边界。

## 执行流程

1. 读取输入与约束。
2. 执行核心动作（建议调用仓库脚本，避免重复造轮子）。
3. 输出结构化结果（结论、证据、下一步）。

## 输出格式

1. 结论（先给可执行结论）。
2. 关键步骤（最少步骤集）。
3. 验证与风险（如何验证、剩余风险）。

## 安全边界

1. 不写入敏感凭据。
2. 不越过项目白名单工具。
3. 需要副作用操作时先做前置检查。
